// Gradle did not like this being in Kotlin

repositories {
    mavenCentral()

    maven {
        name = 'zenoc-repo'
        url = 'https://repo.zenoc.net/repository'
    }

    maven {
        name = 'minecraft-repo'
        url = 'https://libraries.minecraft.net'
    }
}